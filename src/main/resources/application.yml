server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quiz_online
    username: root
    password: 4283
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
